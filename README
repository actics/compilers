После сборки (make) можно вызывать ./compiler arg1 arg2, где аргументы - файл с кодом, и выходной бинарный файл соответственно.
Язык поддерживает комманды scan VAR, print EXPR, VAR = EXPR. Разделителем является точка с запятой или перевод строки. Знак степени - ^.
